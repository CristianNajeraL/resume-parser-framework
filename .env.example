# ============================================================================
# Resume Parser Framework - Environment Configuration
# ============================================================================
# This file documents all available environment variables.
# Copy this file to `.env` and update with your actual values.
#
# Configuration is managed by pydantic-settings, which provides:
# - Automatic type validation
# - Case-insensitive environment variable names
# - Support for .env files
# - Default values for optional settings
# ============================================================================

# ----------------------------------------------------------------------------
# Skills Extractor Settings (LLM-based extraction)
# ----------------------------------------------------------------------------

# Google Gemini API Key (Required for LLM-based skills extraction)
# Get your key at: https://makersuite.google.com/app/apikey
# If not set, skills extraction will fall back to keyword matching
GEMINI_API_KEY=your_api_key_here

# Gemini model to use for skills extraction
# Options: gemini-pro, gemini-1.5-pro, gemini-1.5-flash
# Default: gemini-pro
GEMINI_MODEL=gemini-pro

# Enable keyword fallback if LLM extraction fails
# Default: true
USE_SKILLS_FALLBACK=true

# ----------------------------------------------------------------------------
# Name Extractor Settings (NER-based extraction)
# ----------------------------------------------------------------------------

# spaCy model for Named Entity Recognition
# Options: en_core_web_sm (smallest), en_core_web_md, en_core_web_lg (most accurate)
# Default: en_core_web_sm
# Note: Models must be installed via: uv run python -m spacy download <model_name>
SPACY_MODEL=en_core_web_sm

# ----------------------------------------------------------------------------
# Email Extractor Settings (Regex-based extraction)
# ----------------------------------------------------------------------------

# Custom regex pattern for email extraction (optional)
# Default: Built-in comprehensive email pattern
# EMAIL_PATTERN=your_custom_regex_here
